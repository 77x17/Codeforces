#include <bits/stdc++.h>
using namespace std;

int main(void)
{
    int t; cin >> t;
    while (t--)
    {
        int n, k; cin >> n >> k;
        int a[100005]; 
        FOR(i, 1, n) cin >> a[i];
        
        
    }
    return 0;
}
