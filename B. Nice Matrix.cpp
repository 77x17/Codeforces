#include <bits/stdc++.h>
using namespace std;
#define FOR(i, a, b) for(int i = a; i <= b; i++)

void Palindromes_Rows(void)
{
    if a[i][j] ==
}

void Palindromes_Columns(void)
{

}

int main(void)
{
    freopen("Input.inp", "r", stdin);
    
    int t; cin >> t;
    while(t--)
    {
        int n, m; cin >> n >> m;
        int a[105][105];
        FOR(i, 1, n)
            FOR(j, 1, m) cin >> a[i][j];
        
        cout << a + c << '\n';
    }

    return 0;
}