#include <bits/stdc++.h>
using namespace std;
#define FOR(i, a, b) for(int i = a; i <= b; i++)

int main(void)
{
    int n, k = 0, l = 0; cin >> n;
    int a[35], b = 0, c = 0, d = 0;
    FOR(i, 1, n)
    {
         b = 1;
        c = pow(2, 1); 
        int tong = ((n + 1) * n / 2) / 2;  
        cout << tong;
    }
    //cout << a - b << '\n';
    return 0;
}
