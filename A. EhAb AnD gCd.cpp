#include <bits/stdc++.h>
using namespace std;
#define FOR(i, a, b) for(int i = a; i <= b; i++)

int main(void)
{
	freopen("Input.INP", "r", stdin);
    //freopen("Output.OUT", "w", stdout);
    int t; cin >> t;
    while (t--)
    {
        int x; cin >> x;
        cout << 1 << ' ' << x - 1 << '\n';
    }
    return 0;
}
